// Drizzle schema entry point.
//
// Intentionally empty in the init repo — the first tables are added together
// with the first real EAM feature (see docs/IDEAS.md).
//
// When a table is added, export it here so it shows up in the `schema` object
// passed to `drizzle()` in client.ts.

export {};
